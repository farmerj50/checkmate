[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "npx prisma db push --accept-data-loss --skip-generate || true && npx tsx src/server.ts"
restartPolicyType = "on_failure"
